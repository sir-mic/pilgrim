import 'package:flutter/material.dart';

/// Pilgrim palette — dark, earthy, quiet.
///
/// Dark mode first. Warm near-blacks and parchment tones with muted gold and
/// sage accents. Nothing loud.
class PilgrimColors {
  PilgrimColors._();

  // Dark
  static const darkBackground = Color(0xFF161310);
  static const darkSurface = Color(0xFF201B17);
  static const darkSurfaceHigh = Color(0xFF2A241E);
  static const darkText = Color(0xFFEDE6D8);
  static const darkTextSecondary = Color(0xFFA89E8C);
  static const darkAccent = Color(0xFFC9A876);
  static const darkAccentAlt = Color(0xFF8A9B7D);
  static const darkDivider = Color(0xFF342D26);

  // Light
  static const lightBackground = Color(0xFFF5F0E6);
  static const lightSurface = Color(0xFFFBF8F0);
  static const lightSurfaceHigh = Color(0xFFFFFFFF);
  static const lightText = Color(0xFF2A2620);
  static const lightTextSecondary = Color(0xFF6E6657);
  static const lightAccent = Color(0xFF8A6D3B);
  static const lightAccentAlt = Color(0xFF5E7050);
  static const lightDivider = Color(0xFFE2DAC8);
}
